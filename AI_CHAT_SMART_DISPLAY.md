# AI 聊天智能展示功能说明

## 功能概述

AI 聊天对话框现在支持智能数据展示，能够根据返回的数据类型自动选择合适的展示方式：

1. **普通文本** - 直接显示文字内容
2. **列表数据** - 当有多个数据项时，自动以卡片列表形式展示
3. **单个详情** - 当只有单个数据项时，以详情卡片形式展示
4. **下单卡片** - 当用户要下单时，渲染一个可交互的下单小卡片

## 工作原理

### 后端（scs-agent）

后端 AI 助手在返回数据时，会在回复末尾附加 JSON 格式的结构化数据：

```
这是文字说明部分...

```json
[数据数组或对象]
```
```

支持的 JSON 格式：

1. **列表数据**：JSON 数组
```json
[
  {
    "id": 1,
    "name": "宫保鸡丁",
    "price": 2800,
    "description": "经典川菜",
    "status": 1
  }
]
```

2. **单个详情**：JSON 对象
```json
{
  "id": 1,
  "name": "宫保鸡丁",
  "price": 2800,
  "description": "经典川菜"
}
```

3. **下单卡片**：带有 type 字段的对象
```json
{
  "type": "order",
  "title": "确认订单",
  "items": [
    {"name": "宫保鸡丁", "quantity": 2, "price": 2800}
  ],
  "totalAmount": 5600
}
```

### 前端（index.html 和 menu.html）

前端会自动检测并解析 JSON 数据：

1. 流式响应结束后，调用 `parseAndFormatMessage()` 方法
2. 从 markdown 代码块中提取 JSON
3. 根据数据类型设置 `dataType` 字段：
   - `'text'` - 普通文本
   - `'list'` - 列表数据
   - `'detail'` - 单个详情
   - `'order'` - 下单卡片

4. Vue 模板根据 `dataType` 自动选择对应的展示组件

## 展示效果

### 列表展示
- 每个数据项以卡片形式展示
- 显示标题、价格、状态等关键信息
- 支持 hover 效果和点击交互
- 价格以红色突出显示

### 详情展示
- 单个数据项的完整信息
- 清晰的标签-值对布局
- 重要信息（如价格）高亮显示

### 下单卡片
- 渐变紫色背景，视觉突出
- 订单项列表展示
- 总计金额醒目显示
- "取消"和"确认下单"两个操作按钮
- 点击确认后弹出二次确认对话框

## 使用方法

### 管理端（backend/index.html）

用户可以在管理端的 AI 助手中：
- 查询菜品列表 → 自动以列表展示
- 查看菜品详情 → 自动以详情卡片展示
- 查询订单 → 列表展示订单信息
- 下单操作 → 显示下单卡片供确认

### 用户端（front/menu.html）

用户可以在点餐页面的 AI 助手中：
- 询问推荐菜品 → 列表展示推荐结果
- 查看套餐详情 → 详情卡片展示
- 快速下单 → 下单卡片一键确认

## 技术特点

1. **智能解析**：自动识别 markdown 代码块中的 JSON
2. **容错处理**：JSON 解析失败时回退到纯文本显示
3. **响应式设计**：适配不同屏幕尺寸
4. **流畅动画**：卡片 hover 和点击都有平滑过渡效果
5. **统一风格**：与系统整体设计风格保持一致

## 扩展建议

如需添加新的展示类型：

1. 在后端系统提示词中添加新类型的 JSON 格式说明
2. 在前端 `parseAndFormatMessage()` 中添加新的判断分支
3. 在 Vue 模板中添加对应的展示组件
4. 添加相应的样式定义

## 注意事项

1. 价格单位：后端返回的价格单位为**分**，前端会自动转换为**元**显示
2. 时间格式化：使用 `formatTime()` 方法统一格式化时间显示
3. 状态映射：0 表示禁用/停售，1 表示正常/售卖中
4. 下单功能：目前仅为演示，实际项目中需要对接真实的下单 API
